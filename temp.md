这是一个非常关键的补充。基本的 Tomasulo 算法虽然快，但是它有一个致命弱点：**它无法处理精确异常**，因为它会乱序修改寄存器状态。

为了解决这个问题，我们需要引入 **重排序缓冲区 (Reorder Buffer, ROB)**。以下是按照你的格式整理的讲解：

---

#### **知识卡片：精确异常与 ROB (Reorder Buffer) `[S70]`**

- 要解决的问题（直觉）：
    在基本的 Tomasulo 算法中，指令一算完就立刻修改寄存器（写回）。
    - **场景**：假设指令 A（慢）在指令 B（快）前面。指令 B 先算完并修改了寄存器。紧接着，指令 A 发生了错误（比如除以零）。
    - **灾难**：此时我们需要报错并恢复到 A 执行前的状态。但是，B 已经把寄存器改掉了！这种“覆水难收”的局面导致无法支持**精确异常 (Precise Exceptions)**。
        
- 核心解决方案：重排序缓冲区 (ROB) [S70]：
    
    我们在解码阶段和寄存器堆之间加一个“等候室”（FIFO 队列）。
    
    - **核心思想**：**乱序执行，顺序提交 (Out-of-Order Execution, In-Order Commit)**。
        
- **关键组件：ROB 结构** `[S71]`：
    
    ROB 是一个环形缓冲区，每个条目包含：
    
    - **Instruction Type**：指令类型（是跳转？存数？还是计算？）。
        
    - **Destination**：结果应该写到哪个寄存器。
        
    - **Value**：计算出来的结果（暂时存在这里，还没写到寄存器堆）。
        
    - **Ready**：结果是否已经算好了。
        
#### **知识卡片：ROB 增强后的流水线步骤 `[S72]`**

引入 ROB 后，Tomasulo 的流程变成了 4 步（注意最后一步的变化）：

1. **发射 (Issue/Dispatch)**：
    
    - 从指令队列取指令。
    - **分配 ROB 条目**：按顺序在 ROB 队尾占个座。
    - **发送到保留站**：如果保留站有空，就把指令发过去。如果操作数在寄存器里，就读出来；如果操作数在 ROB 里（还没提交），就建立依赖关系。
        
2. **执行 (Execute)**：
    
    - 如果有操作数没准备好，就监视 CDB（公共数据总线）。
        
    - 操作数齐了就开始计算。
        
3. **写结果 (Write Result)**：
    
    - 计算完成。
        
    - **关键区别**：结果**不再直接写入寄存器文件**，而是**写入 ROB 中对应的条目**，并通过 CDB 广播给其他等待的保留站。
        
    - 此时，虽然算完了，但对软件层面（程序员视角）来说，这条指令还没“生效”。
        
4. **提交 (Commit/Retire)**：
    
    - 这是新增的步骤。只有当一条指令**到达 ROB 的队头**（意味着它前面的所有指令都成功完成了），且它自己也已标记为 `Ready` 时：
        
        - **情况 A（正常）**：把 ROB 里暂存的结果真正写入**寄存器文件 (Architectural Register File)**，并释放 ROB 条目。
        - **情况 B（存数 Store）**：这才真正向内存发出写入请求。
        - **情况 C（发生异常）**：因为它是队头，说明前面的指令都安全了。此时可以直接清空流水线，处理异常，不会有“误杀”或“漏网”的情况。
#### **知识卡片：ROB 的额外好处 - 推测执行 `[S73]`**

- **分支预测 (Branch Prediction)**：
    
    - 有了 ROB，处理器可以大胆地猜测分支方向（Speculation）。
    - **猜对了**：指令在 ROB 里排队，最后顺利提交。
    - **猜错了**：当那条跳转指令到达 ROB 队头时，发现当初猜错了。处理器只需简单地**清空 (Flush)** ROB 中该跳转指令之后的所有条目即可。因为这些错误路径上的指令还没修改寄存器文件，就像它们从未发生过一样。